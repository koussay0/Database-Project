Name: Database Systems Project

Set Up Enviroment:
python3 -m venv venv
source venv/bin/activate
Change port in config.py if needed 

Downloads:
pip install flask
pip install pymysql
pip install Werkzeug

Upload SQL files in "db_design USABLE" to your local database (Schema --> data --> stored procedures)

Run: 
Flask --debug run
